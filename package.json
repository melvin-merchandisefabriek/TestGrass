{
  "name": "test-grass",
  "version": "1.0.0",
  "description": "Docker setup for TestGrass application",
  "scripts": {
    "start": "docker-compose up",
    "build": "docker-compose build",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "dev": "docker-compose up"
  }
}
